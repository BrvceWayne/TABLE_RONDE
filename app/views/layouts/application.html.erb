<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
        <title>TableRonde - On mange où ?</title>
    <meta name="description" content="TableRonde - L'app qui aide les groupes d'amis à choisir où manger ensemble">

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <%= tag.link rel: "manifest", href: webmanifest_path(format: :json) %>
    <%= tag.link rel: "icon", href: image_url("app-icon-512.png"), type: "image/png" %>
    <%= tag.link rel: "apple-touch-icon", href: image_url("app-icon-512.png") %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# Google Maps Places API (New) for address autocomplete %>
    <script async src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_PLACES_API_KEY'] %>&libraries=places&loading=async"></script>
  </head>

  <body>
    <%= render "shared/navbar" unless content_for?(:hide_navbar) %>
    <%= render "shared/flashes" %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
